import { BetaComponent } from '@ling/react-components';
import { AlphaComponent } from '@ling/react-components/alpha';

describe('react-components', () => {
  it('should import beta component', () => {
    BetaComponent();
  })

  it('should import alpha component', () => {
    AlphaComponent();
  })
})